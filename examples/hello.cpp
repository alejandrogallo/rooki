#!/usr/bin/env rooki
// rooki:flags -pedantic -std=c++11
// rooki:flags -x c++
// rooki:spell g++ $flags $f -o $bin

#include <iostream>

int main(int argc, char *argv[])
{
  std::cout << "Hello World rooki" << std::endl;
  return 0;
}
